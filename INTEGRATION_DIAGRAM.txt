╔══════════════════════════════════════════════════════════════════════════════╗
║                   FL MENTAL HEALTH PORTAL - INTEGRATION FLOW                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ PHASE 1: DATA COLLECTION                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    👤 User
     │
     │ Chat + Label Mood (0=negative, 1=positive)
     ↓
  ┌──────────────┐
  │ Portal (MLX) │  → Llama 3.2 generates response
  └──────┬───────┘
         │
         │ Saves to disk
         ↓
  ┌──────────────────────┐
  │ data/chat.jsonl      │  ← Chat history with labels
  │ ─────────────────    │
  │ {"text": "...",      │
  │  "label": 0/1}       │
  └──────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ PHASE 2: FEDERATED LEARNING                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────┐
  │ FL Server            │
  │ (server/server.py)   │
  │                      │
  │ SaveModelStrategy    │◄──┐
  └─────┬────────────────┘   │
        │                    │
        │ Aggregates         │ Sends updates
        │                    │
    ┌───┴────────────────────┴───┐
    │                            │
    ↓                            ↓
┌──────────────┐          ┌──────────────┐
│ FL Client 1  │          │ FL Client 2  │
│              │          │              │
│ DistilBERT   │          │ DistilBERT   │
│ + LoRA       │          │ + LoRA       │
│ + Opacus DP  │          │ + Opacus DP  │
└──────┬───────┘          └──────┬───────┘
       │                         │
       │ Reads labeled data      │
       ↓                         ↓
  ┌──────────────────────────────────┐
  │ data/chat.jsonl (labeled only)   │
  └──────────────────────────────────┘

        After 3 rounds
             ↓
  ┌──────────────────────────────┐
  │ checkpoints/                 │
  │ ├─ fl_mood_classifier.pt     │ ← Final model
  │ ├─ ..._round1.pt             │
  │ ├─ ..._round2.pt             │
  │ └─ ..._round3.pt             │
  └──────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ PHASE 3: MOOD-AWARE RESPONSES                                               │
└─────────────────────────────────────────────────────────────────────────────┘

    👤 User sends message
     │
     ↓
  ┌────────────────────────────────────┐
  │ Portal (portal/app.py)             │
  │                                    │
  │ 1. Load FL model checkpoint        │
  │    _load_fl_model()                │
  │                                    │
  │ 2. Predict mood                    │
  │    predict_mood(text)              │
  │    ├─ Tokenize with DistilBERT    │
  │    ├─ Run inference                │
  │    └─ Return: {label, confidence,  │
  │                 mood: "positive"}  │
  │                                    │
  │ 3. Build mood-aware prompt         │
  │    if mood == "negative":          │
  │      "Be extra empathetic..."      │
  │    else:                           │
  │      "Be encouraging..."           │
  │                                    │
  │ 4. Generate response (MLX Llama)   │
  │    with mood-adapted prompt        │
  └────────────────┬───────────────────┘
                   │
                   ↓
    ┌──────────────────────────────────┐
    │ Response with mood indicator     │
    │                                  │
    │ User: "I'm feeling lonely"       │
    │ [😔 negative (87%)]              │
    │                                  │
    │ Assistant: "It sounds like       │
    │ you're going through a really    │
    │ tough time. I'm here for you..." │
    └──────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ KEY BENEFITS                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  🔒 Privacy-Preserving
     ├─ Raw text never leaves device
     ├─ Only model updates shared
     └─ Differential privacy (Opacus)

  🎯 Personalized
     ├─ Learns from your labels
     ├─ Adapts to your mood patterns
     └─ Improves over time

  🚀 Efficient
     ├─ LoRA adapters (1-2MB)
     ├─ Fast inference (<100ms)
     └─ On-device processing

  🧠 Intelligent
     ├─ Binary mood classification
     ├─ Confidence scores
     └─ Mood-aware prompts

┌─────────────────────────────────────────────────────────────────────────────┐
│ TECHNICAL STACK                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

  Federated Learning:
  ├─ Framework: Flower (v1.9.0)
  ├─ Model: DistilBERT-base-uncased
  ├─ Adapter: LoRA (r=8, alpha=16)
  ├─ Privacy: Opacus differential privacy
  └─ Strategy: FedAvg (2 clients, 3 rounds)

  Portal:
  ├─ Backend: Flask (Python)
  ├─ LLM: Llama 3.2 1B (MLX quantized)
  ├─ Classifier: Trained FL model
  └─ Device: MPS/CUDA/CPU

  Data:
  ├─ Format: JSONL (line-delimited JSON)
  ├─ Labels: 0 (negative), 1 (positive)
  └─ Privacy: Local-only storage

